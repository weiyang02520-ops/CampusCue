# CampusCue 进度

## 2026-08-03

- 已确认本目录是当前运行实例使用的源码根目录。
- 已完成只读审计，确认局域网匿名暴露、SSE 鉴权遗漏、隐私文档失实和前端状态恢复问题。
- 正在执行交付包修复与加固；修改前回滚副本位于交付目录的 `_rollback/20260803-124851/`。
- 当前目录没有 Git 元数据；本轮不初始化仓库或配置远端。

### 修复与交付里程碑

- 看板改为仅监听 `127.0.0.1:6185`，SSE 使用与其它 CampusCue API 相同的会话鉴权；当前运行配置已迁移并完成 loopback 烟测。
- 修复首次加载群竞态、SSE 重连补拉、提醒事件污染任务卡片和乐观操作失败回滚；新增 4 项 Node 状态测试。
- Python 支持范围统一为 `>=3.12,<3.13`，新 `.venv` 使用 Python 3.12.10，旧 Python 3.14 环境保留为 `.venv-py314-backup-20260803`。
- 更正文档中的云端数据流说明；CampusCue 日志不再记录群消息正文、发送者、群号、任务标题、截止时间或推送目标，并新增两项隐私回归测试。
- Python CampusCue 测试 `387 passed`，AstrBot 入口测试 `18 passed`，前端测试 `4 passed`；Ruff check/format、`pip check`、前端生产构建与依赖审计通过。
- 新增确定性打包与解压验证脚本；源码 ZIP 及外层交付 ZIP 已重建，验证未包含 `.env`、`data/`、虚拟环境、依赖、缓存、连续性记录或疑似密钥。
- 最终源码 ZIP SHA-256：`FA5CD0BE1316DF7CE2DF626EA4ABC3707BB84249925B346B9972FE5A85A788FC`；外层交付 ZIP SHA-256：`4831F36F069913F1968D846B91B063F1E7ED6C8D7784E6C616080101A78E9041`。
- 回滚副本：交付目录外置 `_rollback/20260803-124851/`（不纳入公开仓库）。

### 第二轮商业化加固

- 新增 `campuscue.backup` v1 完整备份格式，覆盖任务、来源、群偏好、全局推送设置和抽取溯源；明确排除 API Key、QQ 登录态、NapCat 文件和 AstrBot 平台凭据。
- 完整恢复要求显式替换确认，先验证格式、版本、枚举、大小与重复身份，再在单个数据库事务中替换五张 CampusCue 表；失败回滚测试证明不会留下半恢复状态，成功后统一重建提醒且不携带旧机器 cron ID。
- 主看板、接入面板和任务试推统一使用带超时、同源凭据、鉴权失效、离线和损坏响应分类的 HTTP 客户端；NapCat 安装保留 5 分钟窗口，接入轮询禁止请求堆叠。
- SSE 状态区分首次连接、短时重连和长期离线，浏览器恢复联网后主动补拉任务、来源、统计、提醒与接入状态。
- 模型格式错误的完整响应只保存在本机溯源数据库，异常和提醒日志不再复制模型内容、任务标题或提醒时间。
- CampusCue 测试 `397 passed`，AstrBot 入口测试 `18 passed`，前端状态与网络测试 `9 passed`；Ruff、`pip check`、npm audit、生产构建通过。
- 确定性打包复验通过，新增外置 `SHA256SUMS.txt` 并纳入校验。最终源码 ZIP SHA-256：`7E205CDCB83CD87F5A9903FF74D16F124920A854E0E7AE2BCD0977B26E016181`；外层交付 ZIP SHA-256：`E41575552B874A5E0A0D2C04964AE06D07D6622F8534C702EE54D910530BDC13`。
- 第二轮回滚副本：交付目录外置 `_rollback/20260803-134500/`（不纳入公开仓库）。

### 第三轮 Windows 交付加固

- 新增双击安装、升级和卸载流程，默认安装到 `%LOCALAPPDATA%\CampusCue`，无需管理员权限或手工创建虚拟环境；升级保留 `.env`、`data/` 与 `.venv` 并备份旧程序文件，卸载默认保留私人配置和业务数据。
- 新增运行进程所有权状态，使用安装路径、PID、启动时间和随机校验值共同验证；正常停止先请求应用收尾，陌生进程占用端口时拒绝误杀。
- 启停批处理改为 ASCII 转发壳与 UTF-8 运行控制，移除 GBK 文件必须特殊保存的脆弱约束；PowerShell 5 下安装、打包和验证脚本的 `$PSScriptRoot` 默认值兼容问题已修复。
- 新增隔离运行进程测试与 Windows 安装器生命周期测试，验证首次安装、升级回滚、过期程序文件清理、用户数据保留、受控停止和陌生端口拒绝停止。
- 增加 `CAMPUSCUE_CHANGELOG.md`、`RELEASING_CAMPUSCUE.md` 和灾难恢复顺序；明确当前为哈希校验的受控交付，尚未获得商业代码签名，不冒充已签名安装器。
- CampusCue 全量测试 `399 passed`（含 AstrBot 入口 18 项）；前端测试 `9 passed`，Ruff check/format、`pip check`、`npm audit` 与生产构建通过。
- 嵌套交付验证通过：源码 1592 项、外层 5 项，禁带文件、危险路径、缺失文件、文档漂移、版本漂移和疑似密钥均为 0；解压后关键后端 `103 passed`，安装器生命周期、全新 `npm ci`、前端测试/构建和 Ruff 复验通过。
- 隔离最新版实例 `127.0.0.1:6186` 页面 HTTP 200，`campuscue.backup` v1 API 正常且仅监听 loopback；真实 `127.0.0.1:6185` 进程 PID 未改变。
- 最终源码 ZIP SHA-256：`0630D0AD6C6631CFB4B296B5B4E3A13F96E2F5C40591B4885959ED12FC826ACD`；外层交付 ZIP SHA-256：`B2B9CE1F50EB6B05F47833BD312B111388373BC791A53F15FF60A572273CCFE7`。
- 第三轮回滚副本：交付目录外置 `_rollback/20260803-142058/`（不纳入公开仓库）。

### 第四轮 浏览器 E2E 与重复创建修复

- 在隔离实例 `127.0.0.1:6186` 上用 Playwright 完成全流程浏览器实测：桌面端（新建/编辑/追踪/忽略/完成/撤销/设置/SSE/空态/引导）、移动端 390px、窄屏 599px，共 22 项断言全部通过，无控制台错误、无横向溢出。
- 修复看板「新建」重复创建：API 路径（`routes.py` `POST /tasks`）此前只计算 `dedup_key` 未做去重检查，而 LLM 工具路径有检查——连点或重复录入会生成多个同名任务与多套提醒。现在 API 创建前先查重，重复返回 409；前端 `saveEdit` 增加 `saving` 防连点双提交。实测同名任务第二次创建被拒，同名任务数保持 1。
- 新增 4 项回归测试（TestCreateTask）：手工创建、同名重复被拒、不同截止不算重复、不同群不算重复。API 套件 95 passed。
- 移动端与窄屏验证通过：390px 无横向溢出、新建弹窗与上板正常；599px 无横向溢出。

### 第五轮 设置面板、导入导出与备份恢复验收

- 设置面板 E2E：桌面与移动端打开/保存/关闭、群设置（显示名称）、提醒偏好、全局推送开关（7 个开关）、面板无横向溢出，全部通过。
- 导入导出 API 验收：示例文件导入成功（created 2、reminders_planned 1），缺 kind 文件正确 422 拒绝，导出 kind/version 正确。
- 备份恢复完整循环验收：备份 2 任务 → 删除清空 → 无确认恢复被 422 拒绝（confirm_replace 安全确认生效）→ 带确认恢复成功（任务完整回来含截止时间、自动重建 6 个提醒）。
- 接入面板（含自检状态）、提醒列表渲染均验证通过；桌面/移动端全程无控制台错误。

### 第六轮 提醒链路、试推与无障碍加固

- 提醒调度链路验收：给任务设截止后自动按类型排期（提前 1 天/2 小时/截止时刻共 3 个提醒），重启后 resync 正确恢复并清理过期 job；提醒列表 API 返回正确 fire_at（UTC→北京时间换算无误）。
- 「立即试推」端点验收：无 QQ 平台连接时正确降级——`delivered=False` + 明确原因（"没有平台匹配"）+ 完整推送预览文本（截止时间显示正确），符合无凭据环境的验收边界。
- 修复对话框焦点管理缺陷：新建/编辑（TaskEditor）、设置（SettingsPanel）、接入（SetupPanel）、追踪（TracePanel）四个对话框打开时均未把焦点移入弹窗，键盘与读屏用户会停留在触发按钮上。现统一在打开时 `nextTick` 聚焦对话框容器（sheet/panel，均带 `tabindex="-1"`），并补 `nextTick` 导入。
- 无障碍 E2E 验收 8/8：Tab 可达新建按钮、Enter 打开弹窗、Escape 关闭、无无名按钮、无未标记输入框、图片均有 alt、弹窗焦点入内、html lang 正确。
- 核心流程回归 22/22 通过，确认焦点改动无破坏。

### 第六轮 SSE 断连日志洪水修复与数据隔离

- 定位并修复 Windows 下 SSE 断连导致的日志洪水：`socket.send() raised exception` 来自 Python 3.12 asyncio ProactorEventLoop（`proactor_events.py:353`）在连接丢失后对每次写操作的 warning——10 个页面快速开关即可刷出数十万行，曾使隔离实例卡死（内存 4.6GB、HTTP 无响应）。
- 修复一（应用层）：`routes.py` SSE generator 捕获 `GeneratorExit/ConnectionError/OSError/RuntimeError` 后退出循环，避免在死 socket 上空转；压力测试后实例存活、所有 API 200。
- 修复二（日志层）：`astrbot/core/log.py` 的 `_NOISY_LOGGER_LEVELS` 中 asyncio 从 WARNING 提升为 ERROR（WARNING 级别本身仍会刷屏），并在 `runtime_bootstrap.py` 启动早期同步压制。修复后 10 页面压力测试：socket 异常 0 次、日志 50 行（此前 18 万行）。
- 教训与流程改进：此前 6186 隔离实例未使用独立数据目录（`ASTRBOT_ROOT`），导致删除/恢复测试误清了真实 6185 实例的演示任务数据（非真实用户数据，从未连接过 QQ）。此后所有测试实例统一使用 `ASTRBOT_ROOT=/tmp/cc-test-root` 独立数据目录，与生产数据完全隔离。
- E2E 回归 22/22 通过，桌面/移动端/窄屏功能无回归。

### 第七轮 交付包重建（含第四至六轮修复）

- 重新构建前端（`campuscue/web/dist`，含去重修复与 SSE 防护）并重打包。
- 交付验证全绿：源码 1592 项、外层 5 项；禁带文件、危险路径、缺失、文档漂移、版本漂移、疑似密钥均为 0；SHA256SUMS 校验 passed；解压包确认无 `.venv`/`data/`（符合排除策略）、前端 dist 完整。
- 最终源码 ZIP SHA-256：`C119A41C3F1C8BE23E37F2E380C4BA866F5D698A8BEA45BC8B3632F05F425AF9`；外层交付 ZIP SHA-256：`1109D04BEEE4E138507A17073D11169560014B56A5C668B133C486A6D5134375`。
- 注：校验脚本依赖 `rg`，本机 PATH 未含，需 `export PATH=/c/Users/peng/.cache/opencode/bin:$PATH` 后运行。
