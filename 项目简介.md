# 课讯 CampusCue

**校园通知，自动变成待办。**

2026 年湖北省首届"火山杯"AI 创客大赛 · 青年 AI 创新赛道参赛项目。

---

## 一句话

课讯静默读群里的消息，自己判断哪条是"要办的事"，抽成带截止时间的任务，识别到就推给你——不用 @ 它，不用手动记。

## 要解决的问题

老师在群里发"实验三报告周五晚上12点前交"，不会 @ 任何人。这条消息会被后面两百条闲聊顶上去，学生第二天想起来的时候已经翻不到了。

现有工具都解决不了这件事：

- **Todo App**（滴答清单、微软 To Do）要人手动录入。问题恰恰是没看见，看见了就不会忘。
- **QQ 机器人**（包括课讯的底座 AstrBot）是被动的——不 @ 就不理。而真正要抓的消息从来不 @ 机器人。

课讯的位置就在这个空档里：**从"人去找信息"变成"信息来找人"。**

## 怎么做到的

三级流水线，每一级都是为了让上一级的成本和误报降下来：

| 级 | 做什么 | 成本 |
|---|---|---|
| L1 | 规则预筛。截止关键词 + 时间表达式 + 发信人身份（群主/管理员权重高）+ 长度。挡掉九成闲聊 | 0 token |
| L2 | 命中的消息送大模型，强制输出结构化 JSON：`{类型, 标题, 截止时间, 地点, 需带物品, 置信度, 判断理由}` | 只有一成消息走到这 |
| L3 | 相对时间落地（"周五晚12点"以消息时间戳为基准算成绝对时间）、36 小时内同群相似标题去重、置信度低于 0.7 落到"待确认"而不是直接建任务 | 0 token |

**每条任务都留着原始群消息和 AI 的判断理由**，在看板上点开就能看。这是对"AI 会不会瞎编"的直接回答——不是让人信任它，是让人能核对它。

## 关键设计取舍

**为什么是旁路管道，不是一个插件。** AstrBot 的第一个处理阶段就把没 @ 机器人的群消息丢掉了（`waking_check`）。课讯自研了一条与主对话链路并行的静默管道，抽取在后台跑，主对话不受影响，抽取挂了也不影响正常问答。这是整个项目的技术核心，也是"proactive vs passive"这个产品差异在代码上的落点。

**为什么在识别到的时候推，而不是等临近截止再提醒。** 早期版本是"提前一天提醒"，改掉了——学生需要的是"这条消息有用，你看一眼"，而不是等到快来不及了才被通知。识别即推送，deadline 提醒作为可选的第二层。

**为什么本地部署。** 完整群聊、任务数据库和历史记录都保存在本机，SQLite 文件就在程序旁边，随时能删。L1 在本机挡掉闲聊；只有通过 L1 的候选消息及判断所需的群名、发送者和时间会发送到配置的火山方舟模型。这个边界必须明确告知使用者，而不是把“本地存储”说成“完全不使用云端”。

## 技术构成

Fork 自 AstrBot v4.26.7（AGPL-3.0），自研代码全部在顶层 `campuscue/` 包里，对底座的侵入只有 4 处约 20 余行。

**复用**：Agent tool-loop runner、Provider 抽象、APScheduler 调度、OneBot/NapCat 适配器、SQLModel + SQLite。
**自研**：三级抽取管道、5 张数据表、5 个领域工具、推送与提醒模块、Vue 3 任务看板、JSON 任务导入导出。

对外说"基于开源 Agent 框架二次开发 + 自研校园事务抽取管道"比"我从零写了个 Agent"更可信，也更符合大赛鼓励的开发方式。

## 现在能跑的

- QQ 群接入（NapCat / OneBot v11），静默读全部群消息
- 三级抽取，作业 / 考试 / 比赛 / 活动 / 通知五类
- 识别即推送到指定会话 + Windows 桌面弹窗
- Vue 任务看板：多群切换、按状态筛选、手动新建与修改、抽取溯源、已排提醒一览
- deadline 提醒（可关），免打扰时段，置信度门槛可调
- JSON 任务导入导出（换电脑搬家用）
- 演示回放脚本（不依赖真人在群里发消息）

CampusCue 自动化测试 399 项（含 AstrBot 入口测试 18 项）、前端状态与网络测试 9 项全绿。

## 明确不做（第一版）

FAISS 向量知识库、PDF 通知解析、多用户账号体系、移动端、团队协作催办、语音与图片理解。除 aiocqhttp 和 webchat 之外的 23 个平台适配器全部配置禁用。

这些不是做不了，是排在 MVP 之后。PDF 解析（比赛通知 → 时间线 + 材料清单）依赖已经在了，属于省级决赛增强项。

## 常见提问

**和 Todo App 的区别？** 主动抽取 vs 手动录入。课讯的输入是群消息，不是人的记忆。

**隐私怎么保障？** 完整群聊和数据库只保存在本机，L1 淘汰的消息不会上传；通过 L1 的候选消息会发送到配置的火山方舟模型做结构化抽取。看板默认只监听 `127.0.0.1`，数据库文件可随时删除。

**AI 判断错了怎么办？** 置信度分级 + "待确认"状态 + 全链路溯源。每条任务都能点开看原文和判断理由，错的可以改、可以标"不是任务"。

**和 AstrBot 是什么关系？** 底座复用，垂直层自研。原创部分是一个独立目录，可以直接指着看。

---

上手看 [使用文档.md](使用文档.md)。
