# 课讯 CampusCue 使用文档

给拿到这个源码包的人。推荐走双击安装，第一次约 15 分钟，之后每天只用桌面图标。

项目是干什么的、为什么这么设计，看 [项目简介.md](项目简介.md)。

- [一、安装与升级](#一安装与升级)
- [二、接上 QQ](#二接上-qq)
- [三、日常使用](#三日常使用)
- [四、看板怎么用](#四看板怎么用)
- [五、备份、恢复和任务搬家](#五备份恢复和任务搬家)
- [六、演示回放（不用真人发消息）](#六演示回放不用真人发消息)
- [七、出问题了看这里](#七出问题了看这里)

---

## 一、安装与升级

### 推荐：双击安装

1. 完整解压源码 ZIP，不要在压缩包预览窗口里直接运行。
2. 安装 Python 3.12.x。安装器会拒绝 3.13/3.14；建议从 python.org 安装并勾选 Python Launcher。
3. 双击 **`安装课讯.bat`**。安装器会把程序复制到 `%LOCALAPPDATA%\CampusCue`、创建独立虚拟环境并安装依赖，然后建立桌面和开始菜单快捷方式，全程不需要手敲命令。
4. 首次安装完成后会自动用记事本打开私密配置 `%LOCALAPPDATA%\CampusCue\.env`。填好 `DEEPSEEK_API_KEY`、`ARK_API_KEY` 和 `ARK_MODEL`（或 `ARK_EXTRACT_MODEL`），保存后双击桌面 **CampusCue**。

安装依赖需要联网，3–8 分钟属于正常范围。安装器不申请管理员权限，程序和用户数据都在当前 Windows 账户下。

拿到新版源码包时，再双击新版包里的 **`安装课讯.bat`** 即可升级。升级前会先验证并停止本机 CampusCue，只替换程序文件，保留 `.env`、`data/` 和 `.venv`；旧程序文件会留在 `%LOCALAPPDATA%\CampusCue-upgrade-backups\时间戳`，便于回退。若端口上的进程无法证明属于 CampusCue，升级会拒绝停止或覆盖它。

在 Windows“已安装的应用”中卸载，或双击安装目录的 **`卸载课讯.bat`**。默认删除程序和虚拟环境，但保留 `%LOCALAPPDATA%\CampusCue\.env` 与 `data/`；只有显式使用维护参数 `-PurgeUserData` 才会删除私人数据。

### 备用：源码目录手工安装

#### 1. Python 3.12

必须是 3.12.x，不要用 3.13/3.14。项目元数据和启动脚本都会拒绝其它版本。

```
python --version
```

不是 3.12 就去 python.org 装一个，安装时勾上 “Add python.exe to PATH”。

#### 2. 建虚拟环境、装依赖

在源码包根目录（有 `main.py` 的那一层）开一个 cmd：

```
py -3.12 -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```

依赖比较多，装 3–8 分钟正常。

#### 3. 填 API Key

复制 `.env.example` 成 `.env`，用记事本打开，填这一行：

```
DEEPSEEK_API_KEY=你自己的 key
```

去 platform.deepseek.com 注册领 key。**只填 `.env`，不要写进任何别的文件** —— 配置文件里存的是 `"$DEEPSEEK_API_KEY"` 这个名字，程序运行时才去 `.env` 取真值，所以把整个项目发给别人也不会带出 key。

自动抽取还必须填写 `ARK_API_KEY`，以及 `ARK_MODEL` 或 `ARK_EXTRACT_MODEL`。L1 规则在本机运行；只有通过 L1 的候选消息连同群名、发送者和时间上下文会发送到火山方舟。完整群聊、任务数据库和历史记录仍只保存在本机。

看板默认只监听 `127.0.0.1:6185`，同一局域网的其它设备无法访问。只有明确需要远程访问时才修改 `dashboard.host`，并且必须同时设置 `CAMPUSCUE_REQUIRE_AUTH=1`。

#### 4. 生成默认配置

```
.venv\Scripts\python main.py
```

第一次启动会在 `data/` 下生成 `cmd_config.json` 等文件。看到日志停在监听 6185 端口就说明起来了，这时候按 `Ctrl+C` 关掉。

#### 5. 把对话链路配好

```
.venv\Scripts\python -m campuscue.provision
```

这一步做的事：把 DeepSeek 写进 provider 配置（用 `$DEEPSEEK_API_KEY` 引用）、挂上课讯的人格、开启 aiocqhttp 适配器、把用不到的 23 个平台和 TTS/知识库之类的功能关掉。

想只检查不改动：

```
.venv\Scripts\python -m campuscue.provision --check
```

会逐项打印 `OK` / `MISS`，全绿了再往下走。

---

## 二、接上 QQ

后面的事全在网页上做，不用再碰命令行。

双击 **`启动课讯.bat`**，等十几秒，浏览器会自己打开 <http://127.0.0.1:6185/campus/>。

点右上角 **⚡ 接入**，从上往下：

1. **NapCat** —— 点「安装 NapCat」。会从官方仓库下约 30MB 到 `data/napcat`，并自动把回连地址写好。
   前提是这台电脑**已经装了 QQ**（装了但没登录也行）。装完按钮变成「启动并出码」，点它。
2. **扫码登录** —— 面板上会出现一个字符二维码，用手机 QQ 扫（右上角 ＋ → 扫一扫）。
   建议用小号，别用主号。
3. **QQ 连接** —— 扫完这一栏会变成「已连接」，并显示登录的账号。没变就等几秒，或者看「查看日志」。
4. **监听哪些群** —— 点「同步 QQ 群列表」，群会列出来。
   **新同步进来的群默认都不监听**，要监听哪个自己勾。勾了才会读那个群的消息。
5. **自检** —— 最后看一眼，显示「全部就绪」就完事了。

### 设置消息发到哪

点右上角 **⚙ 设置**，找到「消息发到哪」：

- 选「我的 QQ 私聊」，填自己的 QQ 号，**并且先用这个号加机器人小号为好友**，否则发不出去。
- 「探测到就立刻推」建议开着 —— 这是课讯的主要用法：群里出现一条有用的通知，马上私聊发给你。
- 「电脑弹窗」在 Windows 上会在右下角弹一条系统通知。
- 「临近截止再提醒一次」是第二层，觉得啰嗦可以关，不影响上面的即时推送。

改完点「试一下」，它会先保存再发一条示例消息过去，收到了就说明通了。

顺便可以调的：

- **这个群是什么** —— 告诉 AI 这个群是哪门课的，判断会准一些。这是按群单独设置的。
- **提前多久提醒** —— deadline 提醒的提前量。
- **免打扰** —— 落在这个时段的提醒会挪到结束之后发，不会丢。
- **判断严格程度** —— 置信度门槛，默认 0.7。低于门槛的抽取结果会落到看板顶部的「待确认」，不直接建任务。嫌漏得多就往下调，嫌噪音多就往上调。

---

## 三、日常使用

| 要做的事 | 怎么做 |
|---|---|
| 开机 | 已安装版双击桌面 `CampusCue`；源码版双击 `启动课讯.bat` |
| 关机 | 关掉启动窗口，或从开始菜单运行 `Stop CampusCue`；源码版可双击 `停止课讯.bat` |
| 看日志 | 安装版 `%LOCALAPPDATA%\CampusCue\.tmp\backend.log`；源码版 `.tmp\backend.log` |

启动脚本会先检查 6185 端口。停止脚本只接受带有当前安装路径、启动时间和随机校验值的 PID 状态；遇到陌生进程占用端口时会拒绝停止，避免误杀其它程序。

CampusCue 自己的运行日志只记录消息长度、消息 ID、任务 ID 和耗时等排障信息，不记录群消息正文、发送者、群号、任务标题或推送目标。NapCat 的 debug 日志也默认关闭。日志仍可能包含第三方组件的错误信息，发给别人排障前应先检查，不要连同 `.env` 或 `data/` 一起发送。

那个黑窗口就是开关。正常停止会先通知 CampusCue 自行收尾，超时后也只会结束已验证属于当前安装目录的进程。

绝对路径（如果需要建快捷方式）：

```
C:\...\CampusAgent\启动课讯.bat
C:\...\CampusAgent\停止课讯.bat
```

---

## 四、看板怎么用

顶上三个数：今天到期 / 七天内 / 已逾期。旁边灰色那两个是「被本地规则挡下的消息比例」和「已读群消息数」—— 前者说明大部分闲聊连模型都没送，成本就省在这里。

左上角的下拉框切换群。

任务按时间分组：已逾期 / 今天 / 三天内 / 之后 / 待定时间。每张卡片上：

- **点开溯源** —— 看这条任务是从哪条群消息抽出来的，以及 AI 给的判断理由。**AI 判错了就在这看**，这是整个设计里最要紧的一个按钮。
- **完成 / 忽略 / 重新打开** —— 忽略掉的不会删，勾上底部「显示已完成 / 已忽略」还能翻出来。
- **改** —— 标题、时间、地点、要带的东西都能手改。

「待确认」区在看板最上面：AI 不确定这几条是不是任务，点确认就变成正式任务，点忽略就没了。

「已排提醒」列出所有已经排上队的提醒和触发时间，用来确认提醒真的排上了。

**➕ 新建** 可以完全手动加一条任务，不经过 AI。

---

## 五、备份、恢复和任务搬家

⚙ 设置里有两套工具：「完整备份」用于灾备，「任务搬家」用于只带走任务。

### 完整备份与恢复

「完整备份」用于电脑故障、重装或回退，包含：

- 全部任务及完成状态；
- 群名称、课程名称、监听状态和统计；
- 提醒提前量、免打扰和置信度设置；
- 推送目标与开关；
- L1/L2/L3 溯源记录、候选消息原文和模型原始响应。

它**不包含** `.env`、API Key、QQ 登录态、NapCat 文件或 AstrBot 平台凭据。备份是未加密 JSON，仍包含群消息、发送者和 QQ 推送目标等私人信息，不要上传网盘或发到群里。

点「下载完整备份」保存文件。恢复时点「恢复完整备份」，选文件后看清任务和来源数量再确认。恢复会替换当前全部 CampusCue 数据；服务器会先验证格式、版本、枚举和重复 ID，再用一个数据库事务整体替换，任何一步失败都会保留恢复前的数据。成功后提醒会从任务截止时间重新构建，不会沿用另一台机器的定时任务 ID。

### 灾难恢复顺序

1. 在旧机器的设置页下载“完整备份”，并把 `.env` 另存到受系统加密保护的位置；两者都含敏感信息，不要发群或上传公开网盘。
2. 在新机器双击安装新版 CampusCue，先不要复制整个旧 `data/` 目录，也不要复制 NapCat/QQ 会话文件。
3. 把旧 `.env` 中仍有效的变量写入新安装自动生成的 `.env`；泄露或来源不明的 key 应先在服务商后台轮换。
4. 启动 CampusCue，在接入面板重新安装/启动 NapCat，并使用隔离的小号重新扫码登录 QQ。
5. 确认模型配置和 QQ 连接都就绪后，在设置页恢复“完整备份”。恢复完成后核对任务数、来源数和提醒列表。
6. 用“试一下”发送无敏感内容的测试通知；确认无误后再启用真实群监听。

完整备份不含 `.env`、API Key、QQ 登录态、NapCat 文件和 AstrBot 平台凭据，因此顺序不能倒过来依赖“整目录覆盖”。若恢复失败，事务会保留恢复前数据库；安装升级的程序文件回滚副本与业务数据备份是两套独立机制。

### 只搬任务

**只带任务本身**，群设置、推送设置、抽取记录都不跟着走 —— 新机器上那些要重新配，本来也不该跨机器复制。

导出：点「导出任务」，浏览器下载一个 `课讯任务-2026-07-28.json`，里面是**所有群**的任务，包括已完成的。

导入：点「导入任务」，选那个 JSON 文件。两个选项：

- **全部导入到当前群** —— 两台电脑的群号不一样时勾上，否则任务会落到一个看板上不存在的群里。
- **覆盖已有的同一个任务** —— 默认跳过重复的（按「同群 + 相似标题 + 同一天截止」判重）。勾上则用文件里的内容改写现有任务，用来同步修改。

导完会告诉你「新增 N 条，更新 N 条，跳过 N 条」。

想先试试，包里有个现成的 `示例-导入测试.json`，两条手写任务（一次数学作业 + 一次期末考试），导进去看看长什么样，不合适就点忽略。

导错文件不会出事 —— 不是课讯导出的 JSON 会被直接拒掉，并告诉你为什么。

---

## 六、演示回放（不用真人发消息）

现场演示别指望真有老师在群里发通知。回放脚本走的是**同一条代码路径**，效果和真消息一样：

```
.venv\Scripts\python -m campuscue.replay --list       查看有哪些场景
.venv\Scripts\python -m campuscue.replay             只跑路演那三条（作业/考试/比赛）
.venv\Scripts\python -m campuscue.replay --all        全部灌一遍
.venv\Scripts\python -m campuscue.replay -s homework  只灌作业那条
.venv\Scripts\python -m campuscue.replay --all --pause 3   每条之间停 3 秒，路演时让人看清
.venv\Scripts\python -m campuscue.replay --all --dry-run   只跑 L1，不调模型、不写库
```

场景：

| 名字 | 内容 |
|---|---|
| `homework` | 作业通知 |
| `exam` | 考试通知（能抽出时间 + 地点 + 要带的东西） |
| `competition` | 比赛通知 |
| `activity` | 活动通知 |
| `vague` | 含糊的通知，应该落到「待确认」 |
| `chatter_thanks` / `chatter_laugh` | 闲聊，应该被 L1 规则直接挡掉 |
| `chatter_lunch` | 带时间词的约饭，L1 放过、L2 应判非任务 |
| `peer_talk` | 同学之间的讨论，不是通知 |
| `no_keyword` | 没有关键词的真作业（考验召回） |

中间那四条闲聊和 `peer_talk` 是**负向场景** —— 演示时值得跑一遍，证明它不是见什么都建任务。最后的 `no_keyword` 反过来考召回：一条没有任何关键词的真作业，看它抓不抓得住。

回放的任务落在一个固定的演示群（`demo-7788`）里，在看板左上角的下拉框里能切过去，跟真实群的数据不混。

回放的结果直接写进数据库，所以演示前先 `启动课讯.bat` 把看板开着，任务会自己冒出来。命令行里也会把每一条的判断过程打印出来（L1 是拦是过、置信度多少、判断依据是什么），这段输出本身就很适合当演示内容。

`--dry-run` 只跑 L1 规则，不调模型也不写库 —— 网不好的时候排练用这个。

---

## 七、出问题了看这里

**双击启动脚本，闪一下就没了**
在 cmd 里手动跑一次能看到错误：`cd` 到目录，敲 `启动课讯.bat`。

**说「6185 端口已经被占用」**
先双击 `停止课讯.bat`。若提示“无法证明它属于本目录”，说明端口由旧版或其它程序占用；脚本不会误杀。打开任务管理器确认程序身份后手工关闭，或重启电脑，再启动 CampusCue。

**说「找不到 .venv」**
第一步没做完，回去跑 `python -m venv .venv` 和 `pip install -r requirements.txt`。

**浏览器打开是空白 / 无法访问**
后端还没起来完。等十几秒刷新。还不行就看 `.tmp\backend.log` 最后几十行。

**右上角显示「正在重连」或「已离线 N 分钟」**
看板与实时事件流断开了。网络恢复后会自动补拉任务、来源、统计和提醒；如果一直不恢复，确认启动窗口还在，再看 `.tmp\backend.log`。出现“登录已失效”时，先在 AstrBot 管理面板重新登录，再刷新看板。

**扫码登录后「QQ 连接」一直是未连接**
点 NapCat 那一栏的「重写连接配置」，再「停止」→「启动并出码」重扫一次。如果面板上提示「配置里的地址和当前端口不一致」，就是这个原因。

**群消息读到了，但一条任务都不出**
三个地方看：⚙ 设置里「判断严格程度」是不是调太高了；⚡ 接入里那个群是不是**没勾上监听**；`.env` 里的 `DEEPSEEK_API_KEY` 是不是空的或者余额没了（`.tmp\backend.log` 里会有 401/402）。

**AI 抽出来的东西不对**
点任务卡片上的溯源，看原始消息和判断理由。不是任务就点忽略，抽错了就直接改。这些都不会影响后面的判断 —— 第一版没有做在线学习。

**弹窗没弹**
只有 Windows 有。设置里那个开关如果显示「当前系统不支持」，说明不是 Windows 或者系统通知被关了。

**中文全是乱码**
新版批处理入口只包含 ASCII，并在运行时切换 UTF-8；若仍乱码，通常是终端字体或系统区域设置问题，不影响网页。不要用旧包里的 GBK 启停脚本覆盖新版文件。

---

## 附：目录里都是什么

```
main.py                   入口
requirements.txt          依赖
.env.example              环境变量模板，安装器会生成私密 .env
安装课讯.bat / 卸载课讯.bat   双击安装、升级与卸载
启动课讯.bat / 停止课讯.bat   源码目录日常开关
示例-导入测试.json          导入功能的示例文件

campuscue/                ★ 自研代码全在这里
├── extractor/            三级抽取管道（L1 规则 / L2 大模型 / L3 后处理）
├── tools/                Agent 用的领域工具
├── api/                  看板的后端接口，transfer.py 是导入导出
├── web/                  Vue 任务看板（dist/ 是打好的包，不用装 Node 也能跑）
├── provision.py          配置初始化脚本
└── replay.py             演示回放脚本

astrbot/                  AstrBot 底座，改动只有 4 处约 20 余行
scripts/                  安装、运行控制、打包与交付验证
tests/                    CampusCue 自动化测试 399 项（含 AstrBot 入口 18 项）
data/                     运行时生成 —— 数据库、配置、NapCat。源码包里没有，不要从别处拷
```

改了后端代码直接重启就行。改了 `campuscue/web/` 下的前端代码要重新打包（需要 Node）：

```
npm install --prefix campuscue/web
npm run build --prefix campuscue/web
```

制作交付包（Windows PowerShell）：

```
powershell -ExecutionPolicy Bypass -File scripts\package_campuscue_delivery.ps1
powershell -ExecutionPolicy Bypass -File scripts\validate_campuscue_delivery.ps1
powershell -ExecutionPolicy Bypass -File scripts\test_windows_installer.ps1
```

打包脚本使用固定条目顺序和时间戳，同一份源码会生成相同的 SHA-256，并在交付目录生成外置 `SHA256SUMS.txt`；验证脚本会从外层 ZIP 解出源码包，核对校验文件、路径安全、构建产物、文档一致性和疑似密钥，并拒绝 `.env`、`data/`、虚拟环境、依赖目录、缓存与项目状态记录。

正式发布的版本、变更记录、签名门禁和隔离账号验收要求见 `CAMPUSCUE_CHANGELOG.md` 与 `RELEASING_CAMPUSCUE.md`。当前交付使用 SHA-256 验完整性，尚未使用商业代码签名证书；不得对外宣称为已签名安装器。

跑测试（全量会比较久，分两批）：

```
.venv\Scripts\python -m pytest tests/test_campuscue_extractor.py tests/test_campuscue_store.py tests/test_campuscue_bypass.py tests/test_campuscue_tools.py tests/test_campuscue_reminders.py
.venv\Scripts\python -m pytest tests/test_campuscue_api.py tests/test_campuscue_backup.py tests/test_campuscue_transfer.py tests/test_campuscue_setup.py tests/test_campuscue_notify.py tests/test_campuscue_provision.py
```
