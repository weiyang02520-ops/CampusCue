# CampusCue 变更记录

## 0.1.0 - 2026-08-03

- 将看板默认限制在 `127.0.0.1`，并统一 CampusCue API 与 SSE 会话鉴权。
- 修复首次加载群竞态、SSE 重连补拉、提醒事件污染任务卡和乐观操作失败回滚。
- 增加完整本机备份与事务恢复，明确排除密钥、QQ 会话和 NapCat 状态。
- 统一前端请求超时、离线恢复和可操作错误提示，增加状态与网络回归测试。
- 将支持范围固定为 Python 3.12，并增加确定性 ZIP、SHA-256 与嵌套解压验证。
- 增加 Windows 双击安装、原位升级、默认保留数据的卸载流程，以及带进程归属校验的安全启停控制。

当前交付包通过哈希校验但尚未使用商业代码签名证书签名。面向不受控终端公开分发前，必须按 `RELEASING_CAMPUSCUE.md` 完成签名安装器和真实隔离账号验收。
